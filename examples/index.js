const data1 = "data1"
const data2 = "data2"

/**
 * this is MyClass
 */
export default class MyClass {
  /**
   * @param {number} a - this is a value a.
   * @param {number} b - this is a value b.
   * @return {number} result of the sum value.
   */
  sum(a, b) {
    return a + b
  }
}
